部署方式：
1. 將 index.html、manifest.json、sw.js 上傳到 GitHub Repository 根目錄。
2. Settings → Pages → Deploy from a branch → main / root → Save。
3. 等待網址產生後，用 Safari 開啟並選「加入主畫面」。

更新版本時請三個檔案一起覆蓋。Safari 若仍顯示舊版，可重新整理或刪除主畫面圖示後再加入。